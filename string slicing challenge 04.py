#Ask a user to enter the first line of a nursery rhyme
#Store the length of their input in a variable
#Display the string “This has {the number of letters as above} letters in it”
#Ask a user to enter a starting number (as an integer)
#Ask a user to enter an ending number (as an integer)
#Use the start/end number and store just that section in a variable
#Display the section stored in the variable above

first_line = str(input("Enter the first line of a nusery rhyme>>> "))
